const getAllStudents = async ({ page, size, searchString }) => {};
const insertStudent = async ({
  name,
  email,
  languages,
  gender,
  phoneNumber,
  address,
}) => {};

export default {
  getAllStudents,
  insertStudent,
};
